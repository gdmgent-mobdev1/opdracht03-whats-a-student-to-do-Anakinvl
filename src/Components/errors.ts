/**
 * object is possibly undefined is ? of
 * type string is undefined is not assignable... oplossing in typeof steken en gelijk maken
 * aan string bv:
 *    if(typeof(de code) == 'string)
 * render is toevoegen aan dom .create is het aanmaken
 */
